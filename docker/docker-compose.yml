services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: swarmforge-mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf:ro

